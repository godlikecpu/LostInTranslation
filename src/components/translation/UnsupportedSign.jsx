const UnsupportedSign = ({ letter }) => {
  return (
    <div
      style={{
        fontSize: 25,
        textAlign: "center",
      }}
    >
      {letter}
    </div>
  );
};

export default UnsupportedSign;
